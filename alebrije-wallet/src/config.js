/**
 * Configuration settings for the Alebrije Wallet
 */

const config = {
  DEFAULT_NETWORK: {
    blockchain: 'solana',
    network: 'devnet', // Could be 'mainnet', 'devnet', or 'testnet'
  },
  API_KEYS: {
    // Add API keys here if needed
  }
};

export default config; 